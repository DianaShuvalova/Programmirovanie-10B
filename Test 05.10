Ответы на тестовые вопросы тестирования:
(1) Переменная в программировании - область памяти, у которой адрес можно использовать для доступа к нужной нам информации.
(2) string
(3) Зачем нужен полиморфизм в ООП - чтобы функция могла обрабатывать разные типы данных (вроде бы)
(4) 20
(5) 5
(6) print(sorted(A))
(7) Ошибку, потому что с кортежами нельзя работать так, как со списками.
(8)
def repeat(lst):
    lst2 = []
    for elem in lst:
        if elem in lst2:
            return False
        lst2.append(elem)
    return True

(9) Алгоритм - это комбинация последовательных операций.
Свойства - дискретность, завершаемость, понятность, формальность и результативность (не уверена))
(10)
def prov_color(x1, y1, x2, y2):
    if (abs(x1 - x2) % 2) == (abs(y1 - y2) % 2):
        return True
    else:
        return False
(11) 
...
(12) Записывается так:
x >= y, y >= x
